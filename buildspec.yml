version: 0.1
phases:
  install:
    commands:
      - aws cloudformation package --template-file samTemplate.yaml --s3-bucket hashim123bucket123 --output-template-file outputSamTemplate.yml
artifacts:
  type: zip
  files:
    - samTemplate.yaml
    - outputSamTemplate.yml